import { Component, type ErrorInfo, type ReactNode } from 'react';
export class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(error:Error,info:ErrorInfo){console.error('ExpressMock rendering error',error,info)}render(){if(this.state.failed)return <main className="grid min-h-screen place-items-center bg-slate-50 p-6 text-center"><div><h1 className="text-3xl font-bold text-brand-900">Something went wrong</h1><p className="mt-3 text-slate-600">Please refresh the page. If the problem continues, return to the home page.</p><a className="button button-primary mt-6" href="/">Back to Home</a></div></main>;return this.props.children}}
