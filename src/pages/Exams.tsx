import { Search } from 'lucide-react';
import { useMemo, useState } from 'react';
import { Link, useSearchParams } from 'react-router-dom';
import { SEO } from '../components/SEO'; import { Container, EmptyState, PageHero } from '../components/UI';
import { examCategories, exams } from '../data/exams';

export function Exams() { const [params,setParams]=useSearchParams(); const [query,setQuery]=useState(''); const category=params.get('category')??'All'; const filtered=useMemo(()=>exams.filter(e=>(category==='All'||e.category===category)&&`${e.name} ${e.description}`.toLowerCase().includes(query.toLowerCase())),[category,query]); return <><SEO title="Competitive Exams" description="Browse exam guides and mock-test preparation for banking, SSC, railway, insurance, regulatory and state-level exams." path="/exams"/><PageHero eyebrow="Exam directory" title="Find your examination">Search and filter clear, update-friendly preparation guides for popular Indian competitive examinations.</PageHero><section className="section"><Container><div className="flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm lg:flex-row lg:items-center"><label className="relative flex-1"><span className="sr-only">Search exams</span><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={20}/><input value={query} onChange={e=>setQuery(e.target.value)} className="input pl-11" placeholder="Search by exam name"/></label><div className="flex gap-2 overflow-x-auto pb-1" aria-label="Filter by category">{['All',...examCategories.map(c=>c.name)].map(item=><button key={item} onClick={()=>setParams(item==='All'?{}:{category:item})} className={`whitespace-nowrap rounded-lg px-4 py-3 text-sm font-semibold ${category===item?'bg-brand-900 text-white':'bg-slate-100 text-slate-700 hover:bg-slate-200'}`}>{item}</button>)}</div></div><p className="mt-7 text-sm text-slate-500" aria-live="polite">{filtered.length} {filtered.length===1?'exam':'exams'} shown</p><div className="mt-4 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(exam=><article className="card flex flex-col" key={exam.slug}><p className="text-sm font-bold text-brand-500">{exam.category}</p><h2 className="mt-2 text-xl font-bold text-brand-900">{exam.shortName}</h2><p className="mt-3 flex-1 leading-7 text-slate-600">{exam.description}</p><Link className="mt-6 font-bold text-brand-600 hover:text-brand-800" to={`/exams/${exam.slug}`}>View exam details →</Link></article>)}</div>{!filtered.length&&<div className="mt-5"><EmptyState>No exams match your current search and filter. Try a broader term.</EmptyState></div>}</Container></section></>; }
